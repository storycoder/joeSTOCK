<p id="notice"><%= notice %></p>

<p>
  <strong>Pic:</strong>
  <%= image_tag(@photo.pic) if @photo.pic.present? %>
</p>

<p>
  <strong>User:</strong>
  <%= @photo.user_id %>
</p>

<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
