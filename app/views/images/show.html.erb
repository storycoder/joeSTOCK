<p id="notice"><%= notice %></p>

<p>
  <strong>Img:</strong>
  <%= @image_tag(@image.img) if @image.img.present? %>
</p>

<p>
  <strong>User:</strong>
  <%= @image.user_id %>
</p>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
